<!-- Displays a gallery of recipe thumbnails that link out to individual recipe pages. -->


<%- include ('../partials/boilerplate') %>

<div>
<h3>What's up Dog Pound</h3>
</div>

<% recipes.forEach(recipe => { %>
    <div>
      <h1><%= recipe.name %></h1>
    </div>
  <% } %>


<%- include ('../partials/end') %>